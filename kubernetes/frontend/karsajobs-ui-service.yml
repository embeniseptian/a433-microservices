apiVersion: v1
kind: Service
metadata:
  name: karsajobs-ui-service
spec:
  type: NodePort
  selector:
    app: karsajobs-ui
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30002                  # NodePort kustom untuk UI
